// import { checks, check, Pass, Fail } from './Test';

describe('Test', () => {
  it.only('should do stuff', () => {
    // const actual = {};

    // expect(actual).toStrictEqual({});
    console.log('foo');
  });
});
