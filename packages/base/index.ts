/**
 * Placeholder to shut typescript up about having no entry points that match the
 * default 'includes' in tsconfig.json.
 *
 * We don't want tsconfig.json to have any includes that we would inherit in
 * extending configurations.
 */
